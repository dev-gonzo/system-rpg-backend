# Arquivo de exemplo para variáveis de ambiente
# Copie este arquivo para .env e ajuste os valores conforme necessário

# =============================================================================
# CONFIGURAÇÕES DO BANCO DE DADOS POSTGRESQL
# =============================================================================
DB_HOST=postgres
DB_PORT=5432
DB_NAME=systemrpg_backend
DB_USERNAME=systemrpg_user
DB_PASSWORD=systemrpg_password_2024

# URL completa do banco (gerada automaticamente)
# DATABASE_URL=jdbc:postgresql://${DB_HOST}:${DB_PORT}/${DB_NAME}

# =============================================================================
# CONFIGURAÇÕES JWT
# =============================================================================
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-min-256-bits
JWT_EXPIRATION=86400
JWT_REFRESH_EXPIRATION=604800

# =============================================================================
# CONFIGURAÇÕES OAUTH2
# =============================================================================
OAUTH2_CLIENT_ID=systemrpg-backend-client
OAUTH2_CLIENT_SECRET=your-oauth2-client-secret-change-this

# =============================================================================
# CONFIGURAÇÕES DA APLICAÇÃO
# =============================================================================
SPRING_PROFILES_ACTIVE=docker
SERVER_PORT=8080

# =============================================================================
# CONFIGURAÇÕES DE SEGURANÇA
# =============================================================================
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080
RATE_LIMIT_REQUESTS_PER_MINUTE=60

# =============================================================================
# CONFIGURAÇÕES DE LOG
# =============================================================================
LOG_LEVEL_ROOT=INFO
LOG_LEVEL_SYSTEMRPG=DEBUG
LOG_FILE_PATH=/app/logs/systemrpg-backend.log

# =============================================================================
# CONFIGURAÇÕES DO PGADMIN (OPCIONAL)
# =============================================================================
PGADMIN_DEFAULT_EMAIL=admin@systemrpg.com
PGADMIN_DEFAULT_PASSWORD=admin123

# =============================================================================
# CONFIGURAÇÕES DE DESENVOLVIMENTO
# =============================================================================
# Descomente para habilitar debug remoto
# JAVA_OPTS=-agentlib:jdwp=transport=dt_socket,server=y,suspend=n,address=*:5005

# =============================================================================
# CONFIGURAÇÕES DE PRODUÇÃO
# =============================================================================
# Para produção, considere usar:
# - Secrets management (Docker Secrets, Kubernetes Secrets, etc.)
# - Variáveis de ambiente mais seguras
# - Certificados SSL/TLS
# - Configurações de backup do banco

# Exemplo de configurações adicionais para produção:
# SSL_ENABLED=true
# SSL_KEY_STORE=/app/certs/keystore.p12
# SSL_KEY_STORE_PASSWORD=your-keystore-password
# SSL_KEY_STORE_TYPE=PKCS12

# =============================================================================
# NOTAS IMPORTANTES
# =============================================================================
# 1. NUNCA commite o arquivo .env no repositório
# 2. Use senhas fortes em produção
# 3. Rotacione as chaves JWT regularmente
# 4. Configure backup automático do banco de dados
# 5. Use HTTPS em produção
# 6. Configure monitoramento e alertas
# 7. Implemente rotação de logs
# 8. Use secrets management em produção